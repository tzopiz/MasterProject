# MLService / utils

Общие утилиты для работы с изображениями и массивами, используемые сервисом и скриптами.

- **`image_utils.py`** — вспомогательные функции для 2D/3D данных (см. импорты в `services/` и `tools/`).
