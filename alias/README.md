# alias

Вспомогательные shell-скрипты для локальной разработки (не обязательны для сборки проекта).

| Файл | Назначение |
|------|------------|
| `dev_aliases.sh` | Примеры alias для сборки/запуска Backend и ML (`source` из репозитория в текущий shell). |
| `convert_issue.sh` | Markdown → GitHub Issue (`gh`); см. usage в начале файла. |
